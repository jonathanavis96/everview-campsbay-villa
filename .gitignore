# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP scratch output
.playwright-mcp/

# TypeScript incremental build state
*.tsbuildinfo

# Chrome profile directories a Windows-path Lighthouse run drops into the repo root
C:\\*

# Full-resolution originals from the photographer (45 MP JPEGs + the 4K film).
# Kept in the repo so they are not forgotten on a stray disk, deliberately
# untracked: 678 MB has no business in git. The web-sized derivatives under
# src/assets/everview_photos_webp are what ship.
photo-originals/
